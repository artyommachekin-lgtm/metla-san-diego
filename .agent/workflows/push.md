---
description: Push changes to GitHub automatically
---

// turbo-all

## Steps

1. Stage all changes:
```bash
cd "/Users/user/Downloads/metla San Diego" && git add -A
```

2. Commit with a descriptive message (replace MESSAGE with your commit message):
```bash
git commit -m "MESSAGE"
```

3. Push to GitHub:
```bash
git push
```
